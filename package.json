{
  "name": "grounded",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Grounded — a production-grade RAG starter: cited answers, idempotent ingestion, retries, an \"I don't know\" guardrail, and an eval harness. Provider- and store-agnostic, fully testable offline.",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/server.ts",
    "start": "tsx --env-file-if-exists=.env src/server.ts",
    "ingest": "tsx --env-file-if-exists=.env src/cli/ingest.ts",
    "ask": "tsx --env-file-if-exists=.env src/cli/ask.ts",
    "eval": "tsx --env-file-if-exists=.env src/cli/eval.ts",
    "migrate": "tsx --env-file-if-exists=.env src/stores/pgvector-migrate.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run eval"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "openai": "^4.77.0",
    "pg": "^8.13.1",
    "tsx": "^4.19.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2",
    "vitest": "^3.2.7"
  }
}
