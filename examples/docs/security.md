# Acme security

All customer data is encrypted at rest using AES-256 and in transit using TLS.
Acme is SOC 2 Type II compliant, audited annually by an independent firm.

Passwords are hashed with bcrypt; we never store them in plain text. You can
enable two-factor authentication (2FA) in Settings → Security.
